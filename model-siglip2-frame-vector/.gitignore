# .hf_cache/ only appears with the bind-mount fallback (the named-volume path stores it
# under podman's graphroot); ignore it either way.
.hf_cache/
models/*
.cache
tags/*
test-files/*
test-output/*
__pycache__
*.egg-info
.pytest_cache